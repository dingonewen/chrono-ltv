# @package _global_.serving

host: "0.0.0.0"
port: 8000
workers: 1
reload: false

model_uri: "models:/chrono-ltv-cox/Production"

# Response time SLA in milliseconds
sla_ms: 200

cors:
  allow_origins: ["*"]
  allow_methods: ["GET", "POST"]
